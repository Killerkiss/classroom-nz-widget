# Google OAuth client for a Desktop app. See docs/GOOGLE_SETUP.md.
# Copy to .env and fill in. Never commit the real .env.
#
# For a desktop app the client secret is not truly confidential — it ships in the
# binary, and the flow is protected by PKCE. It is still kept out of the repo.
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
